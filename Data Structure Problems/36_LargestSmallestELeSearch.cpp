//k number of largest(or smallest) elements in an array.
//use sorting.

#include <iostream>
using namespace std;

int main()
{
    int arr[] = { 1, 23, 12, 9, 30, 2, 50 };
    int size = sizeof(arr) / sizeof(arr[0]);

    return 0;
}